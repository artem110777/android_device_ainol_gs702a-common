# Android fstab file.
# The filesystem that contains the filesystem checker binary (typically /system) cannot
# specify MF_CHECK, and must come before any filesystems that do specify MF_CHECK

# <src>          <mnt_point>   <type>   <mnt_flags and options>                                                <fs_mgr_flags>
/dev/block/actc  /system       ext4     ro,noatime,nodiratime,nodev,noauto_da_alloc,barrier=0                  wait
/dev/block/actd  /data         ext4     noatime,nodiratime,nosuid,nodev,noauto_da_alloc,nodelalloc,barrier=0   wait,check
/dev/block/acte  /cache        ext4     noatime,nodiratime,nosuid,nodev,noauto_da_alloc,nodelalloc,barrier=0   wait,check